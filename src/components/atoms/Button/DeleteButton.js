import React from 'react';
import { StyledButton } from 'components/atoms/Button/DeleteButton.style';

const DeleteButton = (props) => <StyledButton {...props}>✖</StyledButton>;

export default DeleteButton;
